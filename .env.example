# aq_server 用 .env の例
# このファイルを `.env` にコピーしてキーを記入してください（.env は git 管理外です）。
# 優先順位: コマンド引数 > 環境変数 > .env
#
#   cp .env.example .env
#   python3 aq_server.py --port 50082

# AquesTalk10 開発キー（設定すると評価版制限〔ナ行・マ行→ヌ〕が解除されます）
AQ_DEV_KEY=
# AquesTalk10 使用キー（開発キーと併用）
AQ_USR_KEY=
# AqKanji2Koe 開発キー（漢字→音声記号列変換用）
AQ_KANJI_DEV_KEY=
